apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gitlab-goproxy.fullname" . }}-test-connection"
  labels:
    {{- include "gitlab-goproxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gitlab-goproxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
